@"
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
"@ | Set-Content -Path "infrastructure\grafana\provisioning\datasource.yml" -Encoding utf8